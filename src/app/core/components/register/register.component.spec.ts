import { ComponentFixture, TestBed } from '@angular/core/testing';

import { RegisterComponent } from './register.component';

describe('RegisterComponent', () => {

  it('should register with correct data ', () => {
    expect(true).toBeTruthy();
  });
  it('should not register if the email address is invalid. ', () => {
    expect(true).toBeTruthy();
  });
  it('should not register if the password is invalid.', () => {
    expect(true).toBeTruthy();
  });
  it('should not register if the passwords do not match. ', () => {
    expect(true).toBeTruthy();
  });
  it('should not register if you do not choose a role. ', () => {
    expect(true).toBeTruthy();
  });
});
